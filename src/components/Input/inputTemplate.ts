const inputTemplate = `
  <div class="{{className}}">
    <span>{{name}}</span>
    <input id='{{id}}' name='{{inputName}}' type='{{type}}' value='{{value}}' />
  </div>
`;

export default inputTemplate;
